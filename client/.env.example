# URL base da API do ERP EVOK ÁUDIO. Em dev local com Docker, a API roda em
# http://localhost:5000 (ver docker-compose.yml / docs/infra/DEPLOY.md na raiz do projeto).
VITE_API_URL=http://localhost:5000
