================================================================================
DISPATCH CODEX CLI — CASE-017
================================================================================

VOCÊ: Copie TUDO isso e cole na aba Codex do VS Code
CODEX: Leia, execute, NÃO TERMINE até passar em TODOS os critérios

================================================================================
PROMPT MASTER CODEX CLI
================================================================================

@workspace

# REMEDIAÇÃO CASE-017: AUD-T01-01 — Rejeitar estoque_atual em POST /items

Você é sanacore-remediation-engineer. Tarefa: bloquear POST /items que tentam 
definir estoque_atual; estoque só vem de inventory_movements.

## Problema (Causa-Raiz Confirmada)

POST /api/items aceita campo `estoque_atual` e grava direto em `products.quantity` 
via mirror, SEM criar entry em `inventory_movements`.

Consequência:
- MRP lê estoque sem histórico de movimento
- 327 itens têm estoque com 0-2 linhas audit_log (vs. 5+ esperadas)
- 6 fluxos críticos dependem de `products.quantity` estar correto

Teste existente mockeia mirror inteiro (não testa invariante).

## Escopo (EXATO — não estenda)

E-1. `server/src/modules/items/presentation/validators/itemValidators.ts:14` 
     → REMOVER `estoque_atual` de `createItemSchema` (Zod)

E-2. `server/src/modules/items/application/use-cases/CreateItemUseCase.ts:41` 
     → DELETAR linha que grava: `item.estoque_atual = dto.estoque_atual`

E-3. `server/src/modules/items/services/itemProductMirrorService.ts:106` 
     → VERIFICAR se ainda toca `estoque_atual` (pode estar safe)

E-4. **Criar teste novo:** `server/tests/unit/create-item-estoque-validation.test.ts`
     → Caso 1: POST com `estoque_atual: 100` → 422 BadRequest
     → Caso 2: POST com `estoque_atual: 0` → 422 BadRequest
     → Caso 3: POST SEM `estoque_atual` → 201 Created, saldo = 0

E-5. VALIDAR: `updateItemSchema` já bloqueia `estoque_atual` (sem alteração)

## Critérios Sucesso (DEVE passar TODOS)

- [ ] POST /api/items com `{ estoque_atual: 100 }` → 422 BadRequest
- [ ] POST /api/items com `{ estoque_atual: 0 }` → 422 BadRequest
- [ ] POST /api/items SEM `estoque_atual` → 201 Created, item.estoque_atual = 0
- [ ] PUT /api/items/:id com `estoque_atual` → 422 (já bloqueava, manter)
- [ ] `npm run test:unit` → 431/431 PASS
- [ ] `npm run test:integration` → 27/27 suites PASS
- [ ] MRP não afetado (continua lendo inventory_movements)
- [ ] Nenhuma alteração banco/schema

## Sequência Execução

**FAÇA NESTA ORDEM (não pule):**

### FASE 1: Análise

Mostre TODOS os 5 arquivos que vai tocar:
- itemValidators.ts
- CreateItemUseCase.ts
- itemProductMirrorService.ts
- Novo teste file
- (E verifica updateItemSchema se precisa)

Depois mostre DIFF EXATO (antes → depois) de CADA arquivo.

Só DEPOIS você aprova.

### FASE 2: Execução

Aguarde aprovação: "yes, proceed"

Então execute todas as mudanças nos arquivos.

### FASE 3: Testes

Depois de modificar TODOS, roda:

```bash
npm run test:unit
npm run test:integration
```

### FASE 4: Relatório

Se TUDO passar:
- [ ] Lista de 5 arquivos modificados (path completo)
- [ ] Número de testes NOVOS criados (+3)
- [ ] Resultado: `npm run test:unit` = 431/431 PASS
- [ ] Resultado: `npm run test:integration` = 27/27 suites PASS
- [ ] Comando para fazer commit

Se ALGO falhar:
- [ ] Mostra erro completo
- [ ] Oferece solução

---

## Desvios Conhecidos (NÃO ESTENDA)

- ❌ Não criar coluna nova no banco
- ❌ Não tocar `updateItemSchema` (já está correto)
- ❌ Não estender escopo a outro lugar onde `estoque_atual` é aceito
- ⚠️ Se encontrar outro place, registre mas não implemente aqui

---

## Armadilhas

1. `itemProductMirrorService` pode estar tocando `estoque_atual`
   Solução: E-3 apenas VERIFICA (não força mudança aqui)

2. Teste existente usa mock que esconde o problema
   Solução: Teste novo usa fake (não mock opaco)

3. `updateItemSchema` já bloqueia (confirmado)
   Solução: Não altere, apenas note na análise

---

## Checklist Final (antes de parar)

Quando TERMINAR E TODOS OS TESTES PASSAREM:

- [ ] Lista de 5 arquivos modificados com path completo
- [ ] Número de testes NOVOS criados (deve ser +3 cases em 1 describe)
- [ ] `npm run test:unit` = 431/431 PASS ✅
- [ ] `npm run test:integration` = 27/27 suites PASS ✅
- [ ] Nenhuma alteração banco/schema ✅
- [ ] updateItemSchema verificado (já bloqueia) ✅
- [ ] Comando para fazer commit (já pronto pra copiar/colar)

---

COMECE AGORA: Mostre TODOS os 5 arquivos que vai tocar.

================================================================================
