# HANDOFF — 2026-08-13 · retomar daqui

Tudo commitado e no `origin/main`. Este arquivo existe para você (e para a
próxima sessão) retomarem sem reconstruir contexto.

---

## Onde o CoreTriad está

**`CORETRIAD OPERATIONALLY VALIDATED` — DECLARADO** em 2026-08-13
(`APR-2026-014` em `coretriad/governance/APPROVALS.md`), com SIM-001 (14/14 PASS)
e SIM-002 (8/8 classes detectadas, 11 findings fechados) como evidência.

O runtime está versionado e viaja entre os dois PCs: 96 agentes prefixados em
`.claude/agents/`, hook `org-isolation.js`, skills e `settings.json`. No outro
computador basta `git pull`.

---

## O que está aguardando VOCÊ

### 1. Abertura do ERP-LEGACY-001 (o próximo passo real)

Comando preparado e **não executado**:
`docs/coretriad/planning/ERP-LEGACY-001_OPENING_COMMAND.md`

Abre **apenas os passos 21–24** da Parte VIII (onboarding, baseline imutável com
tag `legacy-baseline-001`, snapshot técnico, arquitetura AS-IS) e para para novo
gate. Escala: 2.808 arquivos rastreados, 177 suítes, 1.952 testes — ordens de
magnitude acima do SIM-002.

Uma escolha embutida: a skill `/coretriad-onboard` **não existe** (confirmado).
Ou eu executo o fluxo equivalente direto, ou crio a skill antes para o processo
ficar reutilizável. **Diga qual.**

### 2. Dois human gates abertos do SIM-002 (não bloqueiam o ERP)

| Gate | Pergunta |
|---|---|
| **OBS-SIM-002-009** | Quais papéis podem **aprovar fornecedor**? A APR-2026-011 diz "a mesma alçada (`manager`) aplica-se à aprovação", o que pode ser lido como aprovação privativa de `manager`. O código manteve `analyst` até R$ 10.000 pela BR-APR-001, que você nunca revogou. O diretor registrou que o texto não decide sozinho |
| **OBS-SIM-002-006(b)** | Reescrever a APR-2026-013 em números inequívocos. O implementado é "1 envio original + até 3 reenvios = no máximo 4 submissões ao gateway". Se a intenção era 3 submissões totais, é mudança de uma linha — mas normativa |

### 3. Delta audit do SIM-002 (opcional)

O diretor manteve `AUDIT_PASSED = NÃO` para o run `SIM-002-AUD-001`, com **delta
audit como obstáculo único substantivo**. Recomendação registrada: congelar
`ac3e277` e abrir `SIM-002-AUD-002` como delta.

Isso **não bloqueia** o ERP-LEGACY-001 nem contradiz a validação operacional —
`AUDIT_PASSED` julga o produto SIM-002; `OPERATIONALLY VALIDATED` julga a
máquina. Faça se quiser o SIM-002 formalmente aprovado.

### 4. Arquivamento dos simulados (baixa prioridade)

`APR-2026-006` e `APR-2026-010` exigem rodar o `finding-validator` nos findings
que ficaram `PROPOSED` (SIM-001: 004/005/006 · SIM-002: 010/012/013) antes do
arquivamento definitivo — ou descartá-los junto com o ambiente dos simulados.
Ambos estão **fechados como ciclo, não arquivados**.

---

## Dívidas técnicas do próprio CoreTriad

| Item | Onde |
|---|---|
| Transição `RETEST_PASSED → IN_REMEDIATION` não existe na tabela de autoridade | `coretriad/states/STATE_MACHINE.md` — o Director registrou o evento como ocorreu e deixou a conciliação para VeriCore + humano |
| 21 arquivos `.codex/agents/*.toml` espelham o roster pré-CoreTriad | `GAP_ANALYSIS.md` §6 |
| Worktrees `../ERP-Evok-sana-{A..E}` ainda existem | Podem ser removidos com segurança — as 5 branches `sana/SIM-002/*` estão no remoto |

---

## Como retomar

Diga apenas **"retomar handoff"** e eu leio este arquivo, confirmo o estado do
git e sigo pelo item 1. Se quiser ir direto ao ERP, diga **"abrir
ERP-LEGACY-001"** e eu apresento o comando para aprovação final antes de
executar.
