# HANDOFF — 2026-08-13 · retomar daqui

Tudo commitado e no `origin/main`. Este arquivo existe para você (e para a
próxima sessão) retomarem sem reconstruir contexto.

---

## Onde o CoreTriad está

**`CORETRIAD OPERATIONALLY VALIDATED` — DECLARADO** em 2026-08-13
(`APR-2026-014` em `coretriad/governance/APPROVALS.md`), com SIM-001 (14/14 PASS)
e SIM-002 (8/8 classes detectadas, 11 findings fechados) como evidência.

O runtime está versionado e viaja entre os dois PCs: 96 agentes prefixados em
`.claude/agents/`, hook `org-isolation.js`, skills e `settings.json`. No outro
computador basta `git pull`.

---

## O que está aguardando VOCÊ

### 1. Abertura do ERP-LEGACY-001 (o próximo passo real)

Comando preparado e **não executado**:
`docs/coretriad/planning/ERP-LEGACY-001_OPENING_COMMAND.md`

Abre **apenas os passos 21–24** da Parte VIII (onboarding, baseline imutável com
tag `legacy-baseline-001`, snapshot técnico, arquitetura AS-IS) e para para novo
gate. Escala: 2.808 arquivos rastreados, 177 suítes, 1.952 testes — ordens de
magnitude acima do SIM-002.

Uma escolha embutida: a skill `/coretriad-onboard` **não existe** (confirmado).
Ou eu executo o fluxo equivalente direto, ou crio a skill antes para o processo
ficar reutilizável. **Diga qual.**

### 2. Um human gate aberto do SIM-002 (não bloqueia o ERP)

**OBS-SIM-002-009 — quais papéis podem aprovar fornecedor?** A APR-2026-011 diz
"a mesma alçada (`manager`) aplica-se à aprovação", o que pode ser lido como
aprovação privativa de `manager`. O código manteve `analyst` até R$ 10.000 pela
BR-APR-001, que você nunca revogou. O diretor registrou que **o texto não decide
sozinho** e formulou a pergunta para resposta de uma palavra:

- **"continuidade"** → `analyst` até R$ 10.000 + `manager` sem teto (o que está
  implementado); basta o registro, nada muda no código.
- **"restritiva"** → aprovação privativa de `manager`; exige emendar a
  BR-APR-001, remediar e retestar.

> A outra ambiguidade (contagem do limite de retentativa) **foi fechada pelo
> diretor**, não precisa de você: ele decidiu que o texto da APR-2026-013 decide
> sozinho — "reenvio" pressupõe um envio anterior, e "para um pagamento **em
> `failed`**" delimita o universo. 3 reenvios além do envio original é a única
> leitura compatível. Ele delimitou que decidiu que o texto *comporta* o
> comportamento, não que essa era sua intenção.

### 3. Delta audit do SIM-002 (opcional) — obstáculo único do `AUDIT_PASSED`

O diretor manteve `AUDIT_PASSED = NÃO`, mas de natureza **inteiramente
diferente** do "não" anterior: dos quatro fundamentos, três caíram. Zero
CRITICAL e zero HIGH abertos; 11 de 14 fechados. Sobrou um só — o estado
aprovável não é o estado auditado.

E não é formalidade. Duas razões materiais, internas ao run: a §2.2 da coverage
matrix foi **empiricamente refutada** (declarava cobrir "todos os pontos de
decisão de papel" nas mesmas linhas que depois produziram um CRITICAL), e
`ac3e277` contém **código de controle que ninguém auditou** — resolução de
identidade, contador de reenvio, autorização de cancelamento, ciclo de vida de
`failed` — exercitado apenas contra "o defeito antigo sumiu?", nunca contra
"este código novo tem defeitos?".

**Pode ser feito sobre `ac3e277`**: o commit reúne as três propriedades que
faltaram às ondas B e C. Escopo é o diff `f2fcf1c..ac3e277` mais regressão dos
11 fechados — proporcional, não reauditoria integral. As condições estão
enumeradas de forma exaustiva em `audit/runs/SIM-002-AUD-001/30-retest/RETEST_REPORT_WAVE-E.md`
§8, e o diretor declarou que **nada além delas será exigido depois**.

### 3-B. Uma pendência formal que é sua (condição C)

**OBS-SIM-002-010** — o FIND-014 foi remediado **sem passar pelo
`vericore-finding-validator`**, violando a Regra 22 e a condição (a) do veredito
anterior do próprio diretor. Ele **não converteu em bloqueio** (o interesse
protegido pela regra foi atendido por meio mais forte: reprodução empírica do
defeito e severidade fixada por cláusula pré-registrada), mas registrou como
desvio que exige **disposição do CoreTriad Director** — e permitiu validação
retrospectiva.

Culpa minha: pulei o validator ao despachar a WAVE-E direto. Vale registrar,
porque é exatamente o tipo de atalho que o modelo existe para pegar — e pegou.

### 4. Arquivamento dos simulados (baixa prioridade)

`APR-2026-006` e `APR-2026-010` exigem rodar o `finding-validator` nos findings
que ficaram `PROPOSED` (SIM-001: 004/005/006 · SIM-002: 010/012/013) antes do
arquivamento definitivo — ou descartá-los junto com o ambiente dos simulados.
Ambos estão **fechados como ciclo, não arquivados**.

---

## Dívidas técnicas do próprio CoreTriad

| Item | Onde |
|---|---|
| Transição `RETEST_PASSED → IN_REMEDIATION` não existe na tabela de autoridade | `coretriad/states/STATE_MACHINE.md` — o Director registrou o evento como ocorreu e deixou a conciliação para VeriCore + humano |
| 21 arquivos `.codex/agents/*.toml` espelham o roster pré-CoreTriad | `GAP_ANALYSIS.md` §6 |
| Worktrees `../ERP-Evok-sana-{A..E}` ainda existem | Podem ser removidos com segurança — as 5 branches `sana/SIM-002/*` estão no remoto |

---

## Como retomar

Diga apenas **"retomar handoff"** e eu leio este arquivo, confirmo o estado do
git e sigo pelo item 1. Se quiser ir direto ao ERP, diga **"abrir
ERP-LEGACY-001"** e eu apresento o comando para aprovação final antes de
executar.
