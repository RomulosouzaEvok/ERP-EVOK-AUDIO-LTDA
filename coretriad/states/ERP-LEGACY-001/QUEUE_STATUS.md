# QUEUE_STATUS — ERP-LEGACY-001 (remediação pós-auditoria)

Atualizado por VeriCore (Claude Code) em 2026-08-18 (segunda rodada, à tarde),
após rodar o pipeline fixo de remediação (ver `fluxo-remediacao-codex-vericore`
na memória auxiliar): despacho → Codex implementa → segunda opinião estática →
`vericore-audit-verification-runner` (reteste dinâmico real) → veredito do
director. A rodada da manhã deste mesmo dia já havia corrigido divergências
da versão de 17/08 (decisões `APR-2026-054..058` e commits novos não
refletidos). **Antes de confiar neste arquivo em conversas futuras, recomenda-se
reconferir contra `APPROVALS.md` e o HEAD real de cada worktree —
QUEUE_STATUS é um retrato, não a fonte de verdade.**

Legenda de estado:
- `PRONTO_PARA_CODEX` — despacho existe, dono pode colar no Codex agora.
- `RODANDO_NO_CODEX` — dono está executando no Codex neste momento.
- `AGUARDANDO_REVISAO_VERICORE` — implementado, esperando segunda
  opinião/reteste da VeriCore. Não depende do dono.
- `AGUARDANDO_RETESTE_DINAMICO` — segunda opinião estática já aprovou (com
  ou sem ressalva não-bloqueante); falta só reteste dinâmico contra
  `erp_evok_audio_test` para virar `RETEST_PASSED`.
- `BLOQUEADO_DECISAO_DONO` — só avança com decisão humana registrada em
  `coretriad/governance/APPROVALS.md`.
- `DECIDIDO_SEM_DESPACHO` — dono já decidiu (ver APR-2026-XXX), mas nenhum
  código foi escrito ainda — falta preparar e enviar o despacho ao Codex.
- `TRIAGEM_EM_ANDAMENTO` — VeriCore ainda investigando causa-raiz, sem
  despacho ainda.
- `RETEST_PASSED_COM_RESSALVA` (runner) — reteste dinâmico real já executado
  e aprovado, com achado paralelo não-bloqueante registrado. Aguarda só o
  veredito formal do coretriad-director/dono para virar `FINDING CLOSED`.

## Legenda de verificação (coluna "Verificação")

- **VERIFICADO 18/08** — eu (sessão principal, hoje) li o código real e/ou
  rodei o reteste dinâmico nesta rodada. Números e trechos citados vêm de
  execução ou leitura direta feita hoje.
- **HERDADO, NÃO REVERIFICADO** — o veredito citado vem de um documento de
  segunda opinião anterior (data indicada). Eu não li o código nem rodei
  nada sobre este caso nesta sessão — trato o veredito como plausível, não
  como confirmado por mim.

## Fila

| Caso | Finding | Estado | Verificação | Observação |
|---|---|---|---|---|
| CASE-001 | FIND-ERP-001 GRUPO B (idempotência) | `RETEST_PASSED` | **VERIFICADO 18/08 — completo, código lido + reteste dinâmico real** | `APR-2026-054` decidiu P1=Opção A, P2=consumidor externo confirmado, P3=409. Commit `1c19d15`. Segunda opinião: `APROVA`, sem ressalva. Reteste dinâmico: migrations `000048`/`000049` revertem/reaplicam limpo; suítes-alvo 100% verdes (`inventory-movement-idempotency` 3/3, `stock-concurrency` 1/1, `mrp-multi-demand-netting` 3/3, `mrp-quarantine-discount` 7/7, `master-production-plan-cycle` 6/6, `production-start-gate-g6` 5/5, `production-start-manual-tracking-bypass` 4/4, `sale-lot-quality-gate` 9/9); caracterização 9/9 suítes, 66/66 testes, log HTTP real confirma 201→409 na mesma `operation_id`; typecheck/build limpos. As 4 falhas de ambiente encontradas (drift guard, docs-reality-drift, bom-tipo-nao-produtivo, migration 000051 de CASE-012) são de outros casos, não deste diff. **Efeito colateral encontrado e corrigido nesta rodada**: o ciclo up/down das migrations no banco compartilhado `erp_evok_audio_test` expôs que a migration do CASE-012 (`20260818-000051`) não tinha backfill e falhava ao reaplicar em banco com dados — corrigido (commit `59df948`, ver linha do CASE-012), banco restaurado a `up`. Falta só: veredito formal do director. |
| CASE-002 | FIND-ERP-005 (alçada jurídica) | `RETEST_PASSED` | **VERIFICADO 18/08 — completo, código lido + reteste dinâmico real** | F1: li `approvalPolicy.ts` completo — fail-closed em `matched===null` com `effective.length>0` (reason `APPROVAL_POLICY_GAP`), tratamento de `value` nulo/zero antes do matching, contiguidade+teto aberto validados. Commits `5107409`+`8d78882`. P5: nível `approve` exigido para elevação de valor. P4: valores fixos `50000`/`300000` confirmados. Reteste dinâmico: `56/56` unit alvo, `175/175` unit módulo jurídico, `22/22` integração do caso, migration `20260814-000048` revertida/reaplicada cirurgicamente sem afetar as 4 migrations posteriores de outros casos (`migration:status` idêntico antes/depois), typecheck/build limpos. 3 falhas de infraestrutura encontradas (drift dev↔test de contagem de migrations, doc desatualizada, bug pré-existente em BOM/G1) — nenhuma relacionada a este caso. Falta só: veredito formal do director. |
| CASE-008 | AUD-DB-02 | `RETEST_PASSED_COM_RESSALVA` | **VERIFICADO 18/08 — completo** (código lido + reteste dinâmico executado por agente com output real) | Correção 02 (`be6fb32`+`6a52370`). Li `server/index.ts` inteiro: `ShutdownOptions.exitCodeOnSuccess?: number` (default 0), `fatalShutdown` passa `1`, `catch` inalterado. Li `case008-correction02.test.ts` inteiro — 3 cenários exatos. Reteste dinâmico (`vericore-audit-verification-runner`, comandos e output no relatório do agente): `Test Suites: 3 passed, 3 total / Tests: 8 passed, 8 total` (alvo); `10 passed / 54 passed` (módulo); `180 passed, 1 failed / 1962 passed, 1 failed` (server inteiro); typecheck e build exit 0. **Achado não-bloqueante, sem relação com este caso**: `tests/unit/docs-path-reference-guard.test.ts` falha (2 caminhos de doc/dependência inexistentes) — abrir finding de documentação separado. Falta: só o veredito formal do director para `FINDING CLOSED`. |
| CASE-012 | FIND-ERP-007 (rescisão de experiência) | `RETEST_PASSED` | **VERIFICADO 18/08 — completo** (código lido + reteste dinâmico executado por agente com output real) | Commits `fd1cc0b`+`ac22ce8`+`59df948`. Li `employeeContractValidators.ts` (`.superRefine` exige `termination_reason`+`notice_modality` quando `decision==='rescindir'`), `DecideEmployeeContractUseCase.ts` (`export =` único, sem vazamento de interface, cita `APR-2026-057/P14`), diff de `BLOCO_6_RH_API.md` (restrito a §5.2/§6.1), `CreateTerminationProcessUseCase.ts` (bloco `ConflictError` 409 confirmado intocado). Reteste dinâmico original: `211`/`39`/`1` passed, typecheck/build exit 0. **Correção 03 (achada durante reteste do CASE-001)**: a migration `20260818-000051` não tinha `DEFAULT`/backfill e falhava ao reaplicar em banco com dados pré-existentes (`erp_evok_audio_test`, compartilhado) — corrigido com `allowNull:true` → backfill com placeholder auditável → `changeColumn(allowNull:false)`. Reconfirmado após a correção: ciclo up/down/up limpo, 4 linhas pré-existentes receberam placeholder, `211`+`39`+`2`+`1` testes verdes de novo. Banco restaurado a `up`. **Achado colateral não-bloqueante**: bug real em `server/scripts/run-api-suite.cjs` (filtro `--testPathPatterns` não restringe com path posicional) — item de robustez de CI separado, não do finding. Falta: só o veredito formal do director. |
| CASE-013 | FIND-ERP-009 (segregação de função) | `PRONTO_PARA_CODEX` | **VERIFICADO 18/08** (pré-requisito checado, código ainda não existe) | Despacho entregue (prompt colado ao dono em 18/08). **Nenhuma worktree existe ainda** — `git worktree list` não mostra `sana/ERP-LEGACY-001/CASE-013` nem `FIND-ERP-009`. Pré-requisito de sequenciamento com CASE-002 (mesmo arquivo `segregationOfDuties.ts`) confirmado satisfeito: `CASE-002/CASE_STATUS.md:7` tem `REMEDIATION_COMMIT: \`8d78882\`` (corrigido nesta sessão, antes era um placeholder de texto). `APR-2026-058` decidiu P17/P18/P20/P21; P19 aberta, não bloqueia. Despacho recomenda 7 sub-commits fasados. |
| CASE-011 | FIND-ERP-008 (SST/CAT) | `RETEST_PASSED` | **VERIFICADO 18/08 — completo, código lido + reteste dinâmico real** | Commits `1caf9c3`+`8467a99`. Segunda opinião: `APROVA`. D1/D2/D3 confirmados por leitura direta (`EmitCatUseCase.ts:55-67` deriva tipo da gravidade e rejeita incoerência; `emitente` removido de `sst.ts`/`AccidentsTab.tsx`; docs restritas e honestas). Achado adjacente real corrigido: `FOR UPDATE` sem alvo travando `LEFT JOIN` em `findAccidentById` — corrigido com `lock: { of: SstAcidente }`. Reteste dinâmico: 19/19 unit (D1 nos dois sentidos, unicidade, pulo de fim de semana), 2/2 contrato client, integração HTTP real contra Postgres confirmando 201/`obito` sem `emitente`, 103/103 SST completo, 88/88 client completo, typecheck/build limpos dos dois lados. 3 falhas de infraestrutura na suíte de integração inteira são de outros casos (mesmo padrão de drift já visto hoje). D4 (owner SST/RH) segue pendente — decisão humana, não bloqueia código. Falta só: veredito formal do director. |
| CASE-004 | AUD-ALOG-01 (employees + items) | `RETEST_PASSED_COM_RESSALVA` | **VERIFICADO 18/08 — completo, código lido + reteste dinâmico real** | Commit `2c10a80`. Segunda opinião: `APROVA_COM_RESSALVA`. Reteste dinâmico: 16/16 testes-alvo, 93 testes na suíte ampliada, 1967/1968 na suíte completa (única falha pré-existente/não relacionada, `docs-path-reference-guard`). **Sonda dinâmica real refutou a ressalva de autoria via mock**: invocação direta dos controllers reais contra `erp_evok_audio_test` confirmou que `user_id`/`user_name`/`user_ip`/`route`/`method` são de fato persistidos na coluna real de `audit_logs` (não só capturados por mock); `entity_id` de item fica `null` (nunca `NaN`), nenhum dado sensível nas colunas reais. Ressalva do caminho `NaN` confirmada como código morto tanto estática quanto dinamicamente (nenhuma execução real ou hipotética alcança esse caminho). Ressalva remanescente não-bloqueante: a sonda não é teste de integração HTTP versionado no repositório — se quiser essa cobertura permanente, é trabalho futuro de SanaCore. Typecheck/build limpos. Falta só: veredito formal do director. |
| CASE-005 | AUD-AUTHN-01 | `AGUARDANDO_RETESTE_DINAMICO` | **VERIFICADO 18/08 — completo, com execução real de teste** | Commit `7b06404`. Segunda opinião: `APROVA_COM_RESSALVA`. Guard tests rodados de verdade (24/24 passam) — confirmado com poder discriminante real via teste-sonda descartável (não é "verdadeiro por acaso", precedente #6 do HANDOFF). `JWT_SECRET` validado independente de `NODE_ENV` (`runtimeEnv.ts:83-95`, bloco movido para antes do early-return). `server-ci.yml:38-59` sem chave fixa, gerada por execução e mascarada. Suíte completa: 1988 testes, única falha é `docs-path-reference-guard` (pré-existente, artefato de ambiente, já documentado). **Ressalva não-corrigida**: `README.md:49` continua com drift (não menciona proibição de placeholder em todos os ambientes) — gap `C6` assumido, depende de decisão do director sobre ownership de `docs/`. |
| CASE-006 | (rate limit / estoque manual) | `RETEST_PASSED_COM_RESSALVA` | **VERIFICADO 18/08 — completo, código lido + reteste dinâmico real** | Commits `1486c99`/`b1ace53`. Segunda opinião: `APROVA_COM_RESSALVA`. **Marco: o teste de concorrência rodou de verdade pela primeira vez** (estava `describe.skip` desde a criação) — satisfeitos os pré-requisitos de integração (`RUN_INTEGRATION`/`TEST_AUTH_TOKEN`/`TEST_API_URL`), rodou contra Postgres real 2x, sucesso nas duas (exatamente 1 `201`/1 erro, saldo final 0). 5 suítes unitárias específicas: 17/17. Caracterização confirmada restaurada corretamente. Typecheck/build limpos. **Achado de ambiente, não de código**: suíte unitária completa crashou (`TypeError` em `sequelize/lib/dialects/postgres/query.js:47`) **só nesta worktree** — controle decisivo: mesmo comando, mesmo HEAD de auditoria, rodou limpo (176/177) no worktree principal, isolando o problema ao ambiente local da worktree de remediação. Ressalvas não-bloqueantes remanescentes: R6 (tipagem `any`) e R4 (lockfile mobile). Falta só: veredito formal do director. |
| CASE-007 | AUD-AUTHN-03 | `RETEST_PASSED_COM_RESSALVA` | **VERIFICADO 18/08 — completo, código lido + reteste dinâmico real** | Commit `4dc329e`. Segunda opinião: `APROVA`. R1 (`TRUST_PROXY`) confirmado por HTTP real (servidor real, `X-Forwarded-For` distinto → buckets isolados; mesmo IP → 429 na 301ª tentativa). **R3 confirmado falso-positivo por execução real forçada** (script que injeta falha no `store.increment` do rate-limiter durante requisição autenticada real): resultado medido = 500, nunca 401/429 — não mascara autenticação. R2 confirmado real, adiamento consciente documentado, não bloqueante. Suíte unitária: 1962/1963 (única falha pré-existente/não relacionada). Typecheck/build limpos. **Achado colateral relevante (não é regressão deste commit, confirmado por diff)**: `npm run test:integration` completo falha massivamente (86-119/247) por exaustão do `authenticatedUserLimiter` pré-existente — a suíte reusa 1 token em centenas de chamadas em segundos, estourando a cota 300/15min. É um problema operacional de CI que existe hoje no repositório, herdado de outro caso, mas vale atenção separada do director. |
| CASE-009 | FIND-ERP-002 (audit_logs) | `RETEST_PASSED_COM_RESSALVA` | **VERIFICADO 18/08 — completo, SQL executado de verdade contra Postgres real** | Commit `6e84b57`. Segunda opinião: `APROVA_COM_RESSALVA`. Reteste dinâmico R1-R6 executados como SQL real (não simulado): R1 UPDATE bloqueado (mensagem custom capturada), R2 DELETE bloqueado, **R3 DELETE sob `session_replication_role='replica'` bloqueado** (valida a razão de existir do `ENABLE ALWAYS TRIGGER`), R4 INSERT normal passa, R5 `up()` sem erro, R6 `down()` reverte limpo (trigger e função removidos). Confirmado por execução real: `TRUNCATE` não é bloqueado (ressalva já conhecida, limitação arquitetural do Postgres — trigger `BEFORE UPDATE OR DELETE` não cobre TRUNCATE — não é regressão). Banco restaurado ao estado exato inicial. Suíte estática 7/7, typecheck/build limpos. Falta só: veredito formal do director. |
| CASE-010 | FIND-ERP-006 (LGPD) | `AGUARDANDO_RETESTE_DINAMICO` — **com bloqueio de processo, não de código** | **VERIFICADO 18/08 — completo** | Segunda opinião de 17/08: `APROVA_COM_RESSALVA`. Commit `32f637d` (18/08) já entregou a captura real de log exigida. **Confirmado agora: o item de backlog (UI de seleção de política + `GET /retention-policies`) NÃO foi aberto formalmente em lugar nenhum** — buscado em todo `remediation/cases/ERP-LEGACY-001-CASE-010/`, em `REMEDIATION_BACKLOG.md` do projeto e em `APPROVALS.md`: nenhuma ocorrência. Existe só como observação de risco dentro do próprio pacote de evidência (`:225`,`:252`). Confirmado no código: `LgpdTab.tsx` sem seletor de política; só existe `POST /lgpd/retention-policies`, sem `GET`. **Recomendação: o `coretriad-director` deve abrir esse item formalmente (entrada em backlog/decisão) antes de `RETEST_PASSED`/`FINDING CLOSED` — pendência de processo, não de código.** |

## Nota sobre numeração

Não existem CASE-014, 015, 016 ainda — os únicos findings sem caso aberto
eram FIND-ERP-007 e FIND-ERP-009 (CASE-012 e CASE-013), ambos já com
decisão do dono e aguardando despacho.

## Regra de ouro desta fila

Nenhum item `BLOQUEADO_DECISAO_DONO` ou `DECIDIDO_SEM_DESPACHO` avança sem
que a decisão/despacho correspondente esteja de fato registrado —
`DECIDIDO_SEM_DESPACHO` significa que a decisão já existe em
`APPROVALS.md`, mas o despacho ao Codex ainda precisa ser escrito e
enviado. VeriCore não decide por ele (Regra 1 do CLAUDE.md) — nem para
"destravar" a fila, mas também não deve deixar decisão já tomada represada
sem virar despacho.
