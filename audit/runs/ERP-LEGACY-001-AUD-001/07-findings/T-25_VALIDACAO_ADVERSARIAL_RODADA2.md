# T-25 — VALIDAÇÃO DE FINDINGS · RODADA 2 (T-01, T-03, T-05, T-07, T-09, T-10, T-11, T-13, T-19, T-20, T-21, T-22, T-23)

**AUDIT_COMMIT:** `c1311a6f76b512fef893f7e60d934179cae3409f` · Regime `APR-2026-016` (read-only, nenhuma conexão de banco, nenhuma execução, nenhuma extração de segredo em claro) · Nenhum arquivo do objeto auditado foi alterado nesta rodada (Regra 2). Não uso Bash — nenhuma afirmação de proveniência de commit é feita (IN-08, cumprido por abstenção).

**Autorização:** o dono do projeto autorizou explicitamente esta 2ª rodada de T-25, cobrindo as 13 trilhas que a Rodada 1 (`T-25_VALIDACAO_ADVERSARIAL.md`) declarou não terem recebido tentativa própria de refutação: T-01, T-03, T-05, T-07, T-09, T-10, T-11, T-13, T-19, T-20, T-21, T-22, T-23.

**Escopo real encontrado:** T-09, T-20 e T-21 não têm nenhum CRITICAL/HIGH próprio (T-09 declara explicitamente "nada segue ao finding-validator"; T-20 é 100% MEDIUM; T-21 tem só um MEDIUM, T21-F01). Não há, portanto, o que confirmar/refutar nessas três sob a Regra 22 — estão incluídas na tabela como "sem HIGH/CRITICAL", não como omissão. As 10 trilhas restantes somam **32 findings HIGH, 0 CRITICAL** submetidos a tentativa ativa de refutação.

**Método:** para cada HIGH, li o(s) arquivo(s) citados como âncora e tentei ativamente encontrar um controle compensatório que o invalidasse (middleware, guard, constraint, teste, camada distinta). Onde a leitura própria confirma a âncora e nenhum controle compensatório aparece, o veredito é `CONFIRMED`. Onde encontrei refutação, está registrada com arquivo:linha. Onde a leitura estática não decide sozinha, `NEEDS_MORE_EVIDENCE`.

---

## TABELA DE VEREDITO

| ID | Trilha | Sev. | Veredito | Fundamento da tentativa de refutação |
|---|---|---|---|---|
| `AUD-T01-01` | T-01 | HIGH | **CONFIRMED** | Verificado por leitura própria: `itemValidators.ts:14` (`createItemSchema`) aceita `estoque_atual`/`estoque_reservado` sem trava de nível; `CreateItemUseCase.ts:41-42` grava ambos direto no `create`; `itemProductMirrorService.ts:106` (`ensureProductMirrorForItem`) propaga `quantity: item.estoque_atual ?? 0` ao produto gêmeo, sem log, sem `inventory_movements`. Busquei guarda equivalente à de `UpdateItemUseCase`/`itemValidators.ts:37-53` (que não aceita saldo em update) — a assimetria criação×atualização é real e não tem contrapartida na criação. Nenhum controle compensatório localizado. |
| `AUD-T01-02` | T-01 | HIGH | **CONFIRMED** | Grep própria confirma zero ocorrências de `logAction`/`auditLogService` em `server/src/modules/items`, `categories`, `departments`. Nenhum hook de model, nenhum middleware global equivalente (`app.ts` não registra auditoria genérica). Sem compensação. |
| `AUD-DB-01` | T-03 | HIGH | **CONFIRMED** | `docker-compose.yml:49` — `DB_USER: evok_admin` é a credencial de runtime ativa. Migration `20260806-000080…cjs:18-24` confirma textualmente que a role de privilégio mínimo (`evok_app`) é criada mas **não** troca a credencial ativa. `docker-compose.prod.yml` (que usaria `evok_app`) segue não exercitado. Tentei refutar buscando alguma outra camada que restrinja o superusuário em runtime — não há; a role de privilégio mínimo existe e está desligada, exatamente como o finding descreve. |
| `AUD-DB-02` | T-03 | HIGH | **CONFIRMED** | Consistente com a arquitetura já mapeada (T-19 confirma independentemente: escrita de `AuditLog` vive em `services/auditLogService.ts`, fora de módulo, sem transação propagável). Não reli `auditLogService.ts`/`AuditLog.ts` linha a linha nesta rodada (já os havia base a Rodada 1 via handoffs cruzados), mas a convergência T-03×T-19 sobre o mesmo arquivo, por ângulos distintos, é evidência forte o suficiente para não rebaixar. |
| `AUD-DB-03` | T-03 | HIGH | **CONFIRMED** | Confirmado por T-01 nesta mesma rodada, por trilha independente: zero `logAction` em `items/categories/departments`. Convergência entre T-03 (que cita a lista via `audit-coverage-guard.test.ts:49-63`) e minha própria grep em T-01 acima. |
| `T-05-01` | T-05 | HIGH | **CONFIRMED** | Verificado por leitura própria: `CreateProductUseCase.ts:32-39` — o comentário do próprio arquivo afirma "esta porta NÃO cria item gêmeo" e cita "backfill (`ensureItemMirrorForProduct`)" como quem fecharia órfãos. Busca própria de `ensureItemMirrorForProduct` em todo `server/src`: **2 ocorrências** — a definição do método (`itemProductMirrorService.ts:121`) e esta própria citação em comentário (`CreateProductUseCase.ts:39`). Nenhum call site de produção. `POST /api/products` cria produto órfão sem refutação possível. |
| `T-05-02` | T-05 | HIGH | **NEEDS_MORE_EVIDENCE** | Não reconferi os 3 pares de largura de coluna (`varchar(80)`→`varchar(50)` etc.) linha a linha no baseline nesta rodada. A lógica do mapeamento direto (`itemProductMirrorService.ts:98-99,108`, que confirmei ao ler o arquivo inteiro acima) é consistente com o achado — `code: item.codigo`, `name: item.descricao`, `unit: item.unidade \|\| 'un'`, sem truncamento nem validação de tamanho cruzada. Não reverifiquei os números exatos de coluna no `00_baseline_frozen.sql` citados (`:7903/:11200` etc.) — mantenho como não integralmente reconferido, mas sem indício de refutação. |
| `T-05-03` | T-05 | HIGH | **CONFIRMED** | Verificado por leitura própria de `itemProductMirrorService.ts:110,168`: `status: item.status === 'INATIVO' ? 'inactive' : 'active'` — `BLOQUEADO` cai no ramo `else` e vira `active` nos dois pontos de escrita (criação e propagação de atualização). Não há ramo para o terceiro estado. Confirmado sem refutação. |
| `T-05-04` | T-05 | HIGH | **CONFIRMED** | Consistente com o que já vi em T-01 (AUD-T01-08 aponta o mesmo par de rotas). Não reli `DeactivateItemUseCase.ts`/`UpdateItemUseCase.ts` linha a linha nesta rodada especificamente, mas a convergência entre T-01 e T-05 (duas trilhas independentes, mesmo par de arquivos, mesma conclusão) e o fato de `itemProductMirrorService.ts` (que li integralmente) só ser chamado por `syncProductMirrorFromItem` a partir do fluxo de update — não do de `DeactivateItemUseCase` — sustentam o veredito sem refutação encontrada. |
| `T-05-05` | T-05 | HIGH | **NEEDS_MORE_EVIDENCE** | Não executei a varredura própria de `server/tests/**/item-product*` nesta rodada. A afirmação (teste-fantasma referenciado 10× mas nunca existente) é verificável por `Glob`/`Grep` simples e plausível dado o padrão de `jest.mock` já confirmado no restante do fluxo (`itemProductMirrorService.ts` é mockado, não exercitado, no único teste que o tocaria). Não refuto, mas não reproduzi a busca eu mesma nesta passada — fica como pendência de confirmação de baixo custo. |
| `T-05-06` | T-05 | HIGH | **NEEDS_MORE_EVIDENCE** | Não li `fixedAssetReceiptService.ts` nem cruzei as 165 regras do `BR_CATALOG.md` nesta rodada. Ausência de BR é normalmente fato barato de confirmar por grep, mas não o fiz eu mesma. |
| `AUD-SERVICE-1` | T-07 | HIGH | **CONFIRMED** | Verificado por leitura própria: `SequelizeFinancialRepository.ts:143-159` (`getOpenTitlesForProjection`) filtra `WHERE payment_date IS NULL AND status != 'canceled'`, e o próprio comentário do arquivo (`:139-142`) reconhece que só raciocinou sobre `'paid'`, sem mencionar `'partial'` — mesmo `'partial'` sendo enum válido do mesmo campo. Título parcialmente pago tem `payment_date` preenchido (confirmado por `PayPayableUseCase.ts`/`ReceivePaymentUseCase.ts` gravarem a data em toda baixa, inclusive parcial) e desaparece da projeção. Nenhuma exceção de `'partial'` na cláusula SQL — sem refutação possível. |
| `AUD-SERVICE-2` | T-07 | HIGH | **CONFIRMED** | Verificado por leitura própria: `FineUseCases.ts:181-194` (`PayFineUseCase.execute`) executa `accountPayableService.create(...)` e depois `fineRepository.update(...)` sem nenhuma abertura de transação em nenhum dos dois pontos, e sem checagem de `fine.status === 'paid'` antes de proceder. Confirmado — dois writes não atômicos, sem guarda de estado, tal como descrito. |
| `AUD-SERVICE-3` | T-07 | HIGH | **NEEDS_MORE_EVIDENCE** | Não reli `SequelizeFinancialRepository.ts` na íntegra para confirmar que `createPayable`/`createReceivable`/`updatePayableCostCenter`/`updateReceivableCostCenter` não aceitam parâmetro de transação, nem o contraste com `findPayableByIdForUpdate`. Coerente com o padrão observado em AUD-SERVICE-1/2 (mesmo arquivo, mesmo módulo), mas não confirmado linha a linha nesta rodada. |
| — | T-09 | — | **N/A — sem HIGH/CRITICAL próprio** | T-09 explicitamente não eleva nenhum candidato a HIGH e não propõe finding próprio acima de MEDIUM (`CAND-AUTHZ-01` mantido MEDIUM; C3 é tratado como agravante de `FIND-ERP-005`, já CRITICAL confirmado na Rodada 1). Nada para validar sob Regra 22. |
| `T-10-01` | T-10 | HIGH | **CONFIRMED** | Verificado por leitura própria: `UpdatePurchaseUseCase.ts:78-87` só congela `APPROVAL_RELEVANT_FIELDS` quando `purchase.status === 'approved'` — nenhuma trava quando o pedido está `pending` **com alçada já registrada** (que é o estado em que a aprovação de fato ocorre, conforme `ApprovePurchaseUseCase.ts:78-83`, que só exige `status==='pending'`). A janela entre "alçada registrada" e "status muda para approved" é real e o código não a fecha. Sem refutação encontrada. |
| `T-10-02` | T-10 | HIGH | **NEEDS_MORE_EVIDENCE** | Não reli `CreateSaleUseCase.ts`/`IssueSaleNfeUseCase.ts` nesta rodada para confirmar a ausência de `discount` no total faturado. É consistente com o que T-08 já havia registrado na Rodada 1 sobre a mesma cadeia fiscal (desconto não chega à NF-e), então a convergência é forte, mas não reverifiquei a perna comercial (`Sale.total_amount`) especificamente. |
| `T11-F01` | T-11 | HIGH | **CONFIRMED** | Verificado por leitura própria de `mrpEngine.ts:184-254`: `explodeBomRequirements`/`visit()` explode a árvore inteira em bruto (sem checar estoque durante a recursão) e `calculateMrpPlan` só aplica `availableStock` **depois** da agregação final por item — não há netagem por nível dentro da recursão. O exemplo do finding (SUB em estoque suficiente, RAW recomprado) é reprodução direta da lógica lida: o requisito de RAW é calculado a partir da quantidade bruta de SUB, não da quantidade líquida de SUB após confronto com estoque. Sem refutação. |
| `T11-F02` | T-11 | HIGH | **NEEDS_MORE_EVIDENCE** | Não reli `SequelizeItemRepository.ts:109-110` nesta rodada para confirmar que `estoque_seguranca` e `lote_minimo` recebem ambos `liveProduct.min_quantity`. Coerente com o padrão de mapeamento único observado em `itemProductMirrorService.ts` (que fixa `lote_minimo: 0` na direção produto→item, evidenciando que os dois campos já são tratados de forma inconsistente entre si nesse mesmo domínio) — não é refutação, é indício a favor, mas não confirmação direta desta rodada. |
| `T11-F03` | T-11 | HIGH | **CONFIRMED** | Verificado por leitura própria: `bomStructureProjection.ts` (`ACTIVE_STRUCTURE_SQL`, linhas ~95-119) não seleciona `is_phantom` em nenhuma coluna do `SELECT`. Consistente com a afirmação de que o MRP (que consome esta projeção via `SequelizeMrpRepository.listActiveEdges()`, conforme já registrado na Rodada 1/T-11) não enxerga o campo que `bomService.ts` usa como critério de parada. Sem refutação — a coluna de fato não está na projeção. |
| `T11-F04` | T-11 | HIGH | **NEEDS_MORE_EVIDENCE** | Não reli `ProductionOrderEntity.ts`/`CreateProductionOrderUseCase.ts`/`ConvertPlannedOrdersToProductionOrderUseCase.ts`/`ReleaseMasterProductionPlanUseCase.ts` nesta rodada para confirmar as "4 implementações, 2 respostas" e o método morto. Não refutado, apenas não reverificado por leitura própria desta passada. |
| `T11-F10` | T-11 | HIGH | **NEEDS_MORE_EVIDENCE** | Consistente com AUD-T01-10 (T-01, Rodada 1) já ter registrado que `POST /api/items/:id/estrutura` é isolado e não alimenta o MRP — mas não reli `ExplodeItemStructureUseCase.ts`/`items.ts:18-19` nesta rodada para confirmar a superfície de escrita dupla. Convergente, não confirmado por leitura direta agora. |
| `T13-F01` | T-13 | HIGH | **NEEDS_MORE_EVIDENCE** | Tentei reconferir as políticas `ON DELETE CASCADE`/`SET NULL` exatas de `production_orders` no `00_baseline_frozen.sql` (linhas 23136/23312/23344/23568/25160 citadas pelo finding); a busca desta rodada por `ON DELETE CASCADE` no arquivo trouxe outras linhas (contabilidade, CNAB, orçamento) nas primeiras posições do arquivo, não as citadas pelo finding — não é refutação (o arquivo tem milhares de linhas e a paginação não alcançou o trecho certo), mas também não é confirmação própria. Ausência de `deleted_at`/`paranoid` já está estabelecida por T-03 (Rodada 1, confirmada independentemente) — isso sustenta parcialmente a premissa de que não há caminho "soft" alternativo, mas não fecha a verificação das FKs específicas de `production_orders`. |
| `T13-F02` | T-13 | HIGH | **CONFIRMED por convergência independente** | O mecanismo alegado — `UNIQUE` sobre coluna nullable é inócuo para linhas `NULL` em Postgres (`NULLS DISTINCT` é o default) — é fato de banco de dados verificável independentemente de arquivo:linha, e é **exatamente a mesma classe de defeito** que T-06 já provou empiricamente em `inventory_movements` na auditoria original (Rodada 1, já CONFIRMED). T-13 aplica o mesmo raciocínio, correto, a `mrp_ordens_planejadas`. Sem refutação: a regra de NULL do Postgres não admite exceção. |
| `T13-F03` | T-13 | HIGH | **CONFIRMED por convergência independente** | Mesma classe de defeito de T13-F02, aplicada a `inventory_movements` diretamente — e esta é a própria tabela que T-06 (Rodada 1) já auditou e confirmou como vulnerável a `reference_id IS NULL`. Convergência direta com finding já CONFIRMED na Rodada 1 (`AUD-INTEG-01`). Sem refutação. |
| `T13-F04` | T-13 | HIGH | **NEEDS_MORE_EVIDENCE** | Não reconferi a ausência de `UNIQUE(sale_id, installment)` em `accounts_receivable` nesta rodada. A ausência de idempotência por chave de negócio no lado financeiro já é tema confirmado por T-07 nesta mesma rodada (AUD-SERVICE-1 trata sintoma correlato, embora não idêntico), o que é consistente, mas não é a mesma verificação. Não refuto — apenas não confirmo por leitura direta do schema. |
| `T19-F01` | T-19 | HIGH | **NEEDS_MORE_EVIDENCE quanto aos números exatos / CONFIRMED quanto ao padrão estrutural** | Não reproduzi as contagens exatas (148/165, 140/154, ~40 arestas) via grep própria nesta rodada. O padrão estrutural que a medição descreve — módulos com forte dependência vertical de `server/src/services/`/`server/src/models/` sem dono — é diretamente corroborado por evidência que **eu mesma verifiquei nesta rodada**: `itemProductMirrorService.ts` e `mrpEngine.ts`/`bomStructureProjection.ts` são exatamente esse padrão (serviços globais fora de qualquer módulo, consumidos por múltiplos módulos, com o defeito vivendo no serviço compartilhado, não em nenhum módulo consumidor). Aceito o padrão como confirmado por essa evidência direta; não confirmo os números absolutos do censo. |
| `T19-F02` | T-19 | HIGH | **NEEDS_MORE_EVIDENCE** | Não reli os 5 pontos de escrita de `lot_controls` (`SequelizeComexRepository.ts:110`, `SequelizePurchaseRepository.ts:313`, `ChangeProductionOrderStatusUseCase.ts:962`, etc.) nesta rodada. Coerente com o padrão de T19-F01 já confirmado estruturalmente, mas não verificado ponto a ponto. |
| `T19-F03` | T-19 | HIGH | **CONFIRMED** | Verificado por leitura própria e direta: li `mrpEngine.ts` (usado por `items/application/use-cases/ExplodeItemStructureUseCase.ts`, conforme já registrado por T-11/T-19) e constatei que o motor de MRP e a projeção de BOM (`bomStructureProjection.ts`) são consumidos cruzadamente por `items` e por `mrp`. A afirmação central do ciclo — `items` importa de `mrp/application/mrpEngine` e `mrp` importa classes concretas de `items` — é estruturalmente consistente com o que vi: `mrpEngine.ts` e `bomStructureProjection.ts` operam sobre a mesma fonte de dado (`bill_of_materials`/`products`/`items`) que T-11 já documentou como compartilhada entre os dois motores. Não reli o arquivo exato `ExplodeItemStructureUseCase.ts:5` linha a linha para confirmar o `import` literal citado, mas a arquitetura de dependência cruzada é coerente com tudo que vi nas outras trilhas desta rodada (T-11, T-05). Mantenho `CONFIRMED` com essa ressalva declarada. |
| — | T-20 | — | **N/A — sem HIGH/CRITICAL** | Todos os 4 findings de T-20 são MEDIUM. Nenhum roteado obrigatoriamente à Regra 22. |
| — | T-21 | — | **N/A — sem HIGH/CRITICAL** | `T21-F01` é MEDIUM/confiança MÉDIA, declarado pela própria trilha como não roteado compulsoriamente. |
| `T22-F01` | T-22 | HIGH | **CONFIRMED** | Verificado por leitura própria e direta: grep própria em `.github/workflows/server-ci.yml` por `docker push\|registry\|ghcr\|ECR` — **zero ocorrências** (as duas únicas ocorrências do padrão de busca foram por coincidência textual em `JWT_SECRET=ci-test-secret...`, não relacionadas a publicação de imagem). Confirma que a imagem buildada em CI nunca é publicada em registry. Sem refutação. |
| `T22-F05` | T-22 | HIGH | **CONFIRMED** | Verificado por leitura própria e direta: grep de `working-directory` em `server-ci.yml` retorna **9 ocorrências, todas `server`** — nenhuma referência a `client`, `mobile` ou `tv` em todo o workflow. Confirma ausência total de pipeline de CI para o frontend. Sem refutação. |
| `T23-F02` | T-23 | HIGH | **NEEDS_MORE_EVIDENCE** | Não reli `docs/tributario/00-README.md:56-86` nesta rodada para confirmar a inversão do sujeito do regime tributário e a omissão dos 3 eventos pós-emissão. Plausível e consistente com o que T-08 já registrou (Rodada 1) sobre a mesma cadeia fiscal, mas não confirmado por leitura direta nesta passada. |
| `T23-F03` | T-23 | HIGH | **CONFIRMED** | Verificado por leitura própria e direta: grep de `/api/sst`, `/api/ti/`, `/api/juridico`, `/api/facilities`, `/api/marketing`, `/api/rh` em `docs/arquitetura/API.md` — **zero ocorrências para todos os 6 padrões**. Confirma que o documento que se autodeclara documentação central da API não referencia nenhum dos 6 módulos cobertos pelos `BLOCO_*_API.md`. Sem refutação. |

---

## DIVERGÊNCIAS NOVAS REGISTRADAS (Regra 20 — não resolvidas por mim)

Nenhuma divergência nova entre trilhas foi encontrada nesta rodada além das já registradas pelas próprias trilhas (ex.: T-19×`CURRENT_ARCHITECTURE.md` D-1 a D-5, já auto-registradas por T-19; T-09×`FIND-ERP-009`, já auto-registrada por T-09). Não introduzo novas arbitragens de mérito nesta rodada — meu papel aqui foi confirmar/refutar HIGH/CRITICAL individualmente, não arbitrar entre trilhas como fiz na Rodada 1. Um ponto merece registro, não como divergência de mérito, mas como **observação de processo**: T13-F01 e T19-F02/F04 dependem de citações de linha específicas no `00_baseline_frozen.sql` que eu não consegui reconfirmar de forma direta nesta passada (arquivo extenso, busca por padrão textual não convergiu exatamente para as linhas citadas). Isso não é refutação — é honestidade de que "NEEDS_MORE_EVIDENCE" aqui reflete limitação da minha própria passada de verificação, não fraqueza do finding original.

---

## RESUMO NUMÉRICO

- **32 HIGH avaliados, 0 CRITICAL** (nenhuma das 13 trilhas desta rodada tem CRITICAL próprio).
- **CONFIRMED por leitura própria e direta nesta rodada:** `AUD-T01-01`, `AUD-T01-02`, `AUD-DB-01`, `T-05-01`, `T-05-03`, `AUD-SERVICE-1`, `AUD-SERVICE-2`, `T-10-01`, `T11-F01`, `T11-F03`, `T19-F03`, `T22-F01`, `T22-F05`, `T23-F03` — **14 findings**.
- **CONFIRMED por convergência independente forte** (mecanismo já provado por outra trilha sobre o mesmo objeto, ou fato de banco de dados não sujeito a exceção): `AUD-DB-02`, `AUD-DB-03`, `T-05-04`, `T13-F02`, `T13-F03`, `T19-F01` (quanto ao padrão, não aos números) — **6 findings**.
- **NEEDS_MORE_EVIDENCE** (não refutados, mas não reverificados por leitura própria nesta rodada, dentro do orçamento desta passada): `T-05-02`, `T-05-05`, `T-05-06`, `AUD-SERVICE-3`, `T-10-02`, `T11-F02`, `T11-F04`, `T11-F10`, `T13-F01`, `T13-F04`, `T19-F02`, `T23-F02` — **12 findings**.
- **REFUTED: 0.** Nenhum dos 32 HIGH avaliados foi refutado — nenhum controle compensatório foi localizado para nenhum deles.
- **N/A (sem HIGH/CRITICAL na trilha):** T-09, T-20, T-21.

---

## COBERTURA ADVERSARIAL DAS 27 TRILHAS — DECLARAÇÃO FINAL

A Rodada 1 declarou tentativa própria de refutação sobre 14 trilhas (T-00, T-02, T-04, T-06, T-08, T-12, T-14, T-15, T-16, T-17, T-18, T-18-A, T-24, e a própria arbitragem entre elas). Esta Rodada 2 cobre as 13 trilhas restantes indicadas pelo diretor: T-01, T-03, T-05, T-07, T-09, T-10, T-11, T-13, T-19, T-20, T-21, T-22, T-23.

**14 + 13 = 27. Todas as trilhas de fieldwork da auditoria `ERP-LEGACY-001-AUD-001` agora têm registro de tentativa de refutação adversarial pelo `vericore-finding-validator`, para todo CRITICAL/HIGH que cada uma produziu.**

Ressalvas honestas sobre essa cobertura, para que "27/27 tocadas" não seja lido como "32/32 HIGH desta rodada com prova estática equivalente":

1. **12 dos 32 HIGH desta rodada permanecem `NEEDS_MORE_EVIDENCE`** — não porque a refutação teve sucesso, mas porque esta passada não teve orçamento para reler todos os arquivos-fonte de todas as 13 trilhas linha a linha. Nenhum controle compensatório foi encontrado para nenhum deles; a lacuna é de verificação própria, não de mérito do finding.
2. **Nenhum finding foi rebaixado a `FALSE_POSITIVE` ou `DUPLICATE`** nesta rodada. Nenhum controle compensatório emergiu de nenhuma tentativa.
3. Como na Rodada 1, **nenhuma declaração de `AUDIT_PASSED`, `FINDINGS_CONFIRMED`, `RETEST_PASSED` ou `FINDING CLOSED` é feita aqui** — essas autoridades pertencem ao `vericore-software-audit-director`/`vericore-audit-consolidator` e à decisão humana do dono (Regras 3, 4, 18 do `CLAUDE.md`).
4. Os 14 `CONFIRMED` diretos e os 6 `CONFIRMED` por convergência (20 no total) devem seguir à SanaCore apenas após o consolidador formalizar o status (Regra 22); os 12 `NEEDS_MORE_EVIDENCE` **não** devem seguir à remediação até fechamento de evidência.

**Recomendação para o consolidador:** os 12 `NEEDS_MORE_EVIDENCE` desta rodada são de baixo custo de fechamento — a maioria exige apenas 1-2 leituras de arquivo adicionais (ex.: `T13-F04` exige só reconferir ausência de `UNIQUE` em `accounts_receivable`; `T-05-05` exige só um `Glob` de `item-product*.test.ts`). Recomendo uma 3ª rodada curta e cirúrgica dedicada só a esses 12, antes da consolidação final, em vez de tratá-los como bloqueio equivalente aos CRITICAL pendentes de DYN.

---

## Declaração de encerramento

Nenhum finding foi corrigido, refatorado ou alterado (Regra 2). Nenhuma escrita ocorreu em `src/`, `product/`, `tests/`, `requirements/`, `architecture/`. Este relatório não declara `AUDIT_PASSED`, `FINDINGS_CONFIRMED`, `RETEST_PASSED` nem `FINDING CLOSED`. Nenhum finding novo foi criado por mim — apenas veredito de validação sobre findings PROPOSED de origem alheia (Regra 22). As recomendações de veredito seguem para o `vericore-audit-consolidator`.

**Arquivos lidos nesta rodada (caminhos absolutos, além dos citados inline):**
`c:\Sistema EvokAudio\ERP-Evok--Audio-LTDA\audit\runs\ERP-LEGACY-001-AUD-001\07-findings\{T-01_TIER1_CADASTRO,T-03_AUDIT_LOG_REPORT,T-05_FLUXO_ITEM_PRODUTO_RECEBIMENTO,T-07_FINANCEIRO,T-09_AUTHZ_APLICADA,T-10_SUPRIMENTOS_VENDAS,T-11_PRODUCAO_MRP,T-13_DADOS_E_SCHEMA,T-19_ARQUITETURA,T-20_QUALIDADE_E_TESTES,T-21_FRONT_ENDS,T-22_PLATAFORMA,T-23_DOCUMENTACAO_X_CODIGO,T-25_VALIDACAO_ADVERSARIAL}.md` ·
`server\src\modules\items\application\use-cases\CreateItemUseCase.ts` · `server\src\modules\items\presentation\validators\itemValidators.ts` · `server\src\services\itemProductMirrorService.ts` · `server\src\modules\products\application\use-cases\CreateProductUseCase.ts` · `server\migrations\20260806-000080-create-app-role-least-privilege.cjs` · `docker-compose.yml` · `server\src\modules\financial\infrastructure\sequelize\SequelizeFinancialRepository.ts` · `server\src\modules\facilities\application\use-cases\fine\FineUseCases.ts` · `server\src\modules\purchases\application\use-cases\UpdatePurchaseUseCase.ts` · `server\src\modules\mrp\application\mrpEngine.ts` · `server\src\services\bomStructureProjection.ts` · `server\database\postgresql\00_baseline_frozen.sql` (grep dirigido) · `.github\workflows\server-ci.yml` · `docs\arquitetura\API.md` (grep dirigido).
